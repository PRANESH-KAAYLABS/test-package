import React from 'react';
import ReactDOM from 'react-dom/client';
import TestText from "./component/text";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TestText />
  </React.StrictMode>
);
